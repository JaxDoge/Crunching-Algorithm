322. Coin Change


# BSF 试试

class Solution:
	def buildBST(input_list: List[int]):
		length = len(input_list)
		
		
    def coinChange(self, coins: List[int], amount: int) -> int:
    	coinD_num = len(coins)
    	# sort the coins
    	coins.sort()

